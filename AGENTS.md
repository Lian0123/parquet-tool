# AI Collaboration Entry

Before analyzing, changing, or reviewing this repository, read [the AI maintenance entry](docs/ai-maintenance/entry.md).

Rules:

1. Follow the entry's progressive-reading route. Do not load the whole repository first.
2. Treat source code and executed checks as authoritative over documentation.
3. Update the relevant maintenance index when public APIs, CLI behavior, native contracts, builds, tests, or the viewer change.
4. Run the relevant tests and `npm run docs:check`. Record anything not run and why.
5. Do not guess unsupported behavior, commit secrets, or perform unrelated refactors.
