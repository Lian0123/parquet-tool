version: "3.8"

services:
  viewer:
    build:
      context: ./viewer
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data:ro
    environment:
      - PARQUET_DIR=/data
